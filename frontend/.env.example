# Development: leave unset so the browser calls /api/... and Vite proxies to Django (vite.config.js -> http://127.0.0.1:8000).
# Production / direct API: set full backend origin, e.g.:
# VITE_API_URL=https://your-backend.example.com
